# Core
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0
opencv-python>=4.7.0
matplotlib>=3.7.0
scipy>=1.10.0
tqdm>=4.65.0

# Object detection + pose
ultralytics>=8.0.0        # YOLOv8-pose (auto-downloads yolov8n-pose.pt on first run)

# SAM — Segment Anything Model (Facebook Research)
# Install with: pip install git+https://github.com/facebookresearch/segment-anything.git
# or: pip install segment-anything
segment-anything>=1.0

# Checkpoint download (built-in Python; no extra install needed)
# SAM ViT-B: https://dl.fbaipublicfiles.com/segment_anything/sam_vit_b_01ec64.pth
# Run: python run_detection.py --download-sam
